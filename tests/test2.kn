extern {
    // 引入 C 标准库的 printf
    fn printf(format: *u8, ...) i32;
}

fn fib(src: i64) i64 {
    if(src < 2) return src;
    return fib(src - 1) + fib(src - 2);
}

fn main() i32 {
    let a = fib(40);
    printf("%d\n\0".[0].&, a);
    return 0;
}
